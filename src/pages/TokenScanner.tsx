/**
 * Token Scanner page – placeholder for future token scanning (SPL / EVM).
 * Replaces the former Positions tab in the bottom nav.
 */
export function TokenScanner() {
  return (
    <div className="p-4 sm:p-6 max-w-[720px] mx-auto min-h-[50vh] flex flex-col items-center justify-center">
      <h1 className="text-xl font-bold mb-2">Token Scanner</h1>
      <p className="text-white/60 text-sm text-center">
        Coming soon.
      </p>
    </div>
  );
}
